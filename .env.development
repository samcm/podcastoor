# Development environment variables

# API Keys (add your actual keys here)
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# App settings
NODE_ENV=development
CONFIG_PATH=./config
CONFIG_FILE=config.yaml

# Storage paths (local development - no Docker needed)
STORAGE_BASE_DIR=./storage
STORAGE_PUBLIC_URL=http://localhost:3000/files
DATABASE_PATH=./data/podcastoor.db
TEMP_DIR=./tmp

# Public URL for RSS feeds (set to frontend URL for development)
PUBLIC_URL=http://localhost:5173

# Web frontend - use proxy instead of direct URL
# VITE_API_URL=http://localhost:3000/api